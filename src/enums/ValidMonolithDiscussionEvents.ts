export enum ValidMonolithDiscussionEvents {
    ENQUIRY_RECEIVED = 'Enquiry received',
    TRIP_AGREED = '{name} agreed the trip',
    DECLINED_REQUEST = '{name} declined the request',
    CHANGED_TRIP_DETAILS = '{name} changed the trip details',
    NO_LONGER_VALID = 'Trip type set to I don\'t mind because it was no longer valid',
    UPDATED_TRIP_INFORMATION = '{name} updated the trip information',
    DISCUSSION_REOPENED = 'Discussion re-opened by LoveHomeSwap',
    DISCUSSION_CANCELLED = 'Discussion cancelled by LoveHomeSwap',
}
