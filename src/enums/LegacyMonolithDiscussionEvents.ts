export enum LegacyMonolithDiscussionEvents {
    TRIP_AGREED_BY_BOTH_PARTIES = '{name} Trip agreed by both parties',
    BOOKED_RENTAL = '{name} booked the rental',
    SWAP_AGREED = '{name} agreed the swap',
    SWAP_DATES_CHANGED = '{name} changed the proposed swap dates',
    UPDATED_SWAP_INFORMATION = '{name} updated swap information',
    PROPOSED_SWAP_DATES = '{name} proposed swap dates',
    MADE_RENTAL_OFFER = '{name} made a rental offer',
    SET_RENTAL_DATES = '{name} set rental dates',
    SET_RENTAL_PRICE = '{name} set rental price',
    WITHDREW_RENTAL_OFFER = '{name} withdrew the rental offer',
    DECLINED_RENTAL_OFFER = '{name} declined the rental offer',
    DECLINED_RENTAL_REQUEST = '{name} declined the rental request',
    UPDATED_TRIP_INFORMATION_WITH_TYPO = '{name} updated the trip informtaion',
    ADDED_MEMBER_GUARANTEE = '{name} added member guarantee cover for this trip',
    ADDED_SWAP_PROTECTION = '{name} added Swap Protection pack for this trip',
    TRIP_BOOKED = '{name} booked the trip',
}

